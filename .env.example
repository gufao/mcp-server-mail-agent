# MCP Email Server - Environment Variables
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# === ACCOUNTS CONFIG ===
ACCOUNTS_PATH=/app/credentials/accounts.json

# === OUTLOOK SECRETS ===
# Required if using Outlook provider
# Get these from Azure Portal > App registrations
OUTLOOK_CLIENT_ID=
OUTLOOK_CLIENT_SECRET=
OUTLOOK_TENANT_ID=common

# === IMAP/SMTP SECRETS ===
# Required if using IMAP provider
# Use app passwords, not regular passwords!
IMAP_HOST=imap.gmail.com
IMAP_USER=your-email@gmail.com
IMAP_PASSWORD=
SMTP_HOST=smtp.gmail.com

# === ADDITIONAL IMAP ACCOUNTS ===
# Add more variables for additional IMAP accounts
# Reference them in accounts.json as ${VAR_NAME}
# BUSINESS_EMAIL_PASSWORD=
# PERSONAL_IMAP_PASSWORD=
